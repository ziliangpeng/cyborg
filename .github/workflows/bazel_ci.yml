name: Bazel Tests

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  bazel_tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Bazelisk
        uses: bazelbuild/setup-bazelisk@v3

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install

      - name: Install Playwright browsers for CyberVision
        working-directory: apps/cybervision
        run: npx playwright install chromium --with-deps

      - name: Install Playwright browsers for CyberVision Player
        working-directory: apps/cybervision-player
        run: npx playwright install chromium --with-deps

      - name: Run Bazel coverage (non-CUDA)
        run: bazel coverage --config=ci --combined_report=lcov //...

      - name: Generate JavaScript coverage for CyberVision
        working-directory: apps/cybervision
        run: npm run test:unit -- --coverage

      - name: Generate JavaScript coverage for CyberVision Player
        working-directory: apps/cybervision-player
        run: pnpm run test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
          files: bazel-out/_coverage/_coverage_report.dat,apps/cybervision/coverage/lcov.info,apps/cybervision-player/coverage/lcov.info
