# Python dependencies for Bazel build
# Used by MODULE.bazel pip.parse()

# Halftone app dependencies
pillow>=10.0.0
numpy>=1.24.0
click>=8.1.0

# TinyLLM dependencies
tinygrad
huggingface_hub
safetensors
tiktoken
transformers>=4.40.0  # For OPT tokenizer (need newer version for pre-built wheels)

# CUDA benchmark framework dependencies
torch
# Note: triton is installed via system pip, not bazel-managed pip
# (bazel pip.parse() has issues with triton's index)
